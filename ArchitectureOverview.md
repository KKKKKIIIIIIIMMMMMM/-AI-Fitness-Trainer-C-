# AI Fitness Trainer - Core Architecture

โปรเจกต์ **AI Fitness Trainer** ถูกออกแบบมาด้วยสถาปัตยกรรมแบบแยกส่วน (Modular Architecture) เพื่อให้จัดการ ซ่อมแซม และขยายขีดความสามารถ (เช่น การเพิ่มท่าใหม่ๆ) ได้ง่าย โดยแต่ละไฟล์มีหน้าที่เฉพาะเจาะจงเหมือนแผนกต่างๆ ในยิมฟิตเนส ดังนี้ครับ:

## 🧩 1. อธิบายหน้าที่ของแต่ละไฟล์ (Core Components)

### `MainForm.h` (ตัวจัดการศูนย์กลาง & หน้าตาแอป)
*   **หน้าที่:** เปรียบเสมือนผู้จัดการใหญ่ (UI Controller / Front-end) คอยวาดหน้าต่างโปรแกรม ปุ่มกด เมนูต่างๆ สร้างหน้าจอแสดงผลกล้อง และรับคำสั่งจากการคลิกของผู้ใช้ (Event Handlers)
*   **การทำงาน:** ทำงานด้วยวงจรหลัก (Main Loop) 2 เส้นขนานกัน (Multi-threading):
    1.  **Camera & Inference Thread:** สั่งดึงภาพจากกล้อง โยนให้บรรดาโมเดล AI มะรุมมะตุ้มประมวลผลจับจุด และคอยคำนวณท่าอยู่เบื้องหลัง
    2.  **UI Timer Tick:** คอยตื่นขึ้นมาทุกๆ เสี้ยววินาที เพื่อเอาผลลัพธ์จาก AI (เช่น นับ Rep, แจ้งเตือนข้อความ, เปลี่ยนสีแถบพลัง) มาอัปเดตลงบนจอให้คนเล่นเห็นแบบ Real-time พร้อมวาด Animation สาธิตทับลงไป

### `PoseEstimator.h` / `.cpp` (ตาของ AI - ระบบวิเคราะห์พิกัดกระดูก)
*   **หน้าที่:** เป็นด่านหน้าในการ "มองคนให้ออกมาเป็นจุดเส้นปริศนา 33 จุด (Landmarks)" 
*   **การทำงาน:** ใช้โมเดล Deep Learning ระดับโลกอย่าง YOLOv8-Pose (ตระกูล ONNX) โหลดภาพจากกล้องเข้าไป แล้วพ่นตำแหน่งพิกัดแกน X, Y และค่าความมั่นใจ (Confidence) ของอวัยวะต่างๆ (เช่น ตา, จมูก, ไหล่, ศอก, เข่า) กลับมาเป็นอาร์เรย์ตัวเลข เพื่อป้อนให้ไฟล์คณิตศาสตร์คำนวณต่อ

### `BiomechanicsMath.h` / `.cpp` (สมองซีกตรรกะ - ระบบคณิตศาสตร์สรีระ)
*   **หน้าที่:** เป็นสมุดคณิตศาสตร์ รวมสูตรตรีโกณมิติทั้งหมด คอยแปลงจุดอ้างอิง X,Y เป็น "องศาข้อต่อ" (Joint Angles)
*   **การทำงาน:** รับจุดกระดูก 3 จุด (เช่น ไหล่-ศอก-ข้อมือ) มาเข้าสูตร `atan2` เพื่อหาว่าตอนนี้แขนงอกี่องศา รวมถึงมีฟังก์ชันฉลาดๆ อย่าง `poseExerciseHint` ที่เอาองศาทุกจุดมาหาค่าความน่าจะเป็นว่า *"ลักษณะกางแขนย่อขาแบบนี้ คนน่าจะกำลังเล่นท่าไหนอยู่"* (Sensor Fusion)

### `ExerciseController.h` / `.cpp` (โค้ชฟิตเนส - ระบบวิเคราะห์และนับครั้ง)
*   **หน้าที่:** รับบทเทรนเนอร์ส่วนตัว คอยวิเคราะห์ "จับผิดจังหวะและฟอร์มการเล่น" (Form & Rep Logic) รวมถึงเก็บสถิติตลอดเซ็ต
*   **การทำงาน:** มีฐานข้อมูล (Array) ชื่อ `CONFIGS` ไว้จดจำเงื่อนไขของท่าออกกำลังกายทั้ง 23 ท่า เช่น *"Squat ต้องย่อเข่าต่ำกว่า 90 องศาถึงจะนับลง และยืนขึ้นเกิน 160 องศาถึงจะนับจบ 1 ครั้ง"* ถ้าผู้เล่นย่อไม่ลึกพอ มันจะตีตราว่าฟอร์มนี้เป็น "Partial (ครึ่งๆ กลางๆ)" ถ้าทำสมบูรณ์ก็นับ "Full" พร้อมสั่งตีเส้นสีเขียว

### `ExerciseClassifier.h` / `.cpp` (ตาวิเศษ - AI แยกแยะชนิดท่าทาง)
*   **หน้าที่:** คอยทายว่า *"ผู้ใช้ทำท่าอะไรอยู่"* หากผู้ใช้ลืมกดเลือกชื่อท่าก่อนเริ่มเล่น (Video / Frame Classification)
*   **การทำงาน:** ดมกลิ่นพฤติกรรมการขยับย้อนหลังสักพัก (Temporal logic) หรือดูจากเฟรมปัจจุบัน เพื่อพยายามเดาว่านี่คือท่า "Push-Up" หรือ "Lunge" กันแน่ แล้วคอยสะกิดบอก `ExerciseController` ให้สลับไปใช้กติกาการนับของท่านั้นๆ

### `InjuryRiskAssessor.h` / `.cpp` (หมอเวชศาสตร์ - ระบบเซฟอาการบาดเจ็บ)
*   **หน้าที่:** คอยจับผิดท่าทางอันตราย เช่น หลังค่อม, หัวเข่าโยก, ลงน้ำหนักผิดจุด ដើម្បីป้องกันอาการบาดเจ็บ
*   **การทำงาน:** สมมติโหมด Deadlift มันจะคอยดูองศาของ "คอ" "ไหล่" และ "สะโพก" ถ้าไหล่ทรุดลงไปต่ำกว่าเส้นเกณฑ์ (แปลว่าหลังค่อมคร่อมบาร์เบล) มันจะรีบส่งสัญญาณ Warning ตัวแดงขึ้นเตือนกลางจอทันที

### `CameraHandler.h` / `.cpp` (นายทวารพอร์ตกล้อง)
*   **หน้าที่:** จัดการเกี่ยวกับการเปิด/ปิด เลนส์กล้อง และดึงเฟรมภาพออกมาทีละแชะ
*   **การทำงาน:** รองรับการดึงภาพจากกล้อง Webcam ธรรมดาต่อสาย USB (Index 0, 1) และสามารถดึงภาพจากกล้องวงจรปิด หรือแอปมือถือผ่านระบบโครงข่าย (RTSP/HTTP IP Camera) ได้อย่างลื่นไหล

### `UserProfile.h` (สมุดพกนักกีฬา)
*   **หน้าที่:** โครงสร้างข้อมูล (Struct) เก็บค่าโปรไฟล์ส่วนตัว (น้ำหนัก, ส่วนสูง, เพศ, นามแฝง, เป้าหมายการเล่น)
*   **การทำงาน:** เอาข้อมูลส่วนบุคคลไปเข้าสมการเพื่อคำนวณแคลอรี่เป้าหมาย (Calories Burned Goal) และเป้าหมายจำนวน Set/Rep ที่เหมาะสมกับสภาพร่างกายของ User คนนั้นๆ โดยเฉพาะ

---

## 🚀 2. การทำงานร่วมกัน (The AI Workflow Execution)

โครงสร้างวงจรการทำงานระดับ Real-time (สิ่งที่เกิดขึ้น 30 เฟรมต่อ 1 วินาที):

**(1) Fetching Phase:**
`MainForm` สั่ง `CameraHandler` ดูดภาพปัจจุบัน 1 เฟรม (Frame) ออกมาจากกล้อง

**(2) Machine Learning Phase:**
ส่งภาพเข้า `PoseEstimator` (ประมวลผลผ่าน GPU) เพื่อกระเทาะเปลือกรูปภาพ หาจุดกระดูกให้ออกมาเป็นโครงจุด (Skeletons) 33 จุดลอยๆ 

**(3) Mathematics Phase:**
ส่งจุด 33 จุดนั้นเข้าห้องแลป `BiomechanicsMath` เพื่อสกัดจับหา "องศา (Angles)" ของข้อต่อที่สำคัญของท่านั้นๆ 

**(4) Coaching & Safegaurd Phase:**
ส่งองศาที่วัดได้ไปให้คู่หูตัวตึง:
*   `ExerciseController`: ตรวจสอบกติกาว่าองศานี้ผ่านเกณฑ์ (Threshold) ของท่าหรือยัง ถ้าย่อสุด ถือว่าเป็น "Down-State" ถ้าดึงตัวขึ้นสุด ถือว่าเป็น "Up-State" เพื่อบวก +1 Rep
*   `InjuryRiskAssessor`: เช็คว่าองศาปลอดภัยไหม ถ้าเข่าเลยปลายเท้าตอน Squat มากเกินไป สั่งกระพริบเตือนสีแดง "Adjust Position"

**(5) Rendering Phase:**
`MainForm` รับผลคะแนนการวิเคราะห์ทั้งหมด กลับมาแปลงเป็นข้อความกราฟิกสีสันสวยงาม (UI) 
*   วาดโครงเส้นกระดูก (Skeleton Lines) ทับลงบนตัวคน 
*   เปลี่ยนสายเส้นเป็นสีเขียว/แดง ตามความถูกต้อง
*   เลื่อนแท่ง Progress Bar 
*   รันเฟรมรูป GIF สาธิตแบบโปร่งแสงทับไปพร้อมๆ กัน

*กระบวนการทั้งหมดนี้จบภายใน 15-30 มิลลิวินาที ทำให้แอปเสมือนกระจกวิเศษที่มีเทรนเนอร์คอยจ้องมองเราอยู่ตลอดเวลา!*
